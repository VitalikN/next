import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contacts | Next App",
};

export default function Contacts() {
  return <h2 className="title"> Welcom to Contacts page</h2>;
}
